{
  "name": "discord-stream-activity",
  "version": "1.0.0",
  "private": true,
  "description": "Discord Activity for low-latency screen sharing via LiveKit SFU",
  "scripts": {
    "dev": "pnpm --parallel --filter ./apps/* run dev",
    "build": "pnpm --filter ./packages/* run build && pnpm --filter ./apps/* run build",
    "typecheck": "pnpm --recursive run typecheck",
    "lint": "pnpm --recursive run lint",
    "clean": "pnpm --recursive exec rm -rf dist node_modules .vite *.tsbuildinfo"
  },
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  },
  "packageManager": "pnpm@9.0.0"
}
